# ShipSentry Lite Report

> Generated by ShipSentry Lite v0.1.0 on 2026-08-06 00:23 UTC. Static analysis only; findings require human verification.

## Summary

| High | Medium | Low | Total |
|---:|---:|---:|---:|
| 2 | 4 | 2 | 8 |

## Findings

### 1. [HIGH] Release build may be debuggable

**Evidence**

```text
app/build.gradle.kts:10:            isDebuggable = true
```

**Recommended fix:** Remove `debuggable true` from release variants and verify the merged release manifest.

### 2. [HIGH] Cleartext network traffic is enabled

**Evidence**

```text
app/src/main/AndroidManifest.xml:2:    <application android:allowBackup="true" android:usesCleartextTraffic="true" />
```

**Recommended fix:** Disable cleartext globally. If an exception is unavoidable, scope it to the exact development host.

### 3. [MEDIUM] Application backup is explicitly enabled

**Evidence**

```text
app/src/main/AndroidManifest.xml:2:    <application android:allowBackup="true" android:usesCleartextTraffic="true" />
```

**Recommended fix:** Set an explicit backup policy and exclude tokens, identifiers, and other sensitive files.

### 4. [MEDIUM] WebView exposes a JavaScript bridge

**Evidence**

```text
app/src/main/java/dev/ship/risky/MainActivity.kt:10:        webView.addJavascriptInterface(bridge, "Native")
```

**Recommended fix:** Only expose a minimal annotated interface to trusted content, validate navigation, and avoid loading untrusted pages.

### 5. [MEDIUM] WebView permits file access

**Evidence**

```text
app/src/main/java/dev/ship/risky/MainActivity.kt:9:        webView.settings.allowFileAccess = true
```

**Recommended fix:** Disable file access unless it is required and tightly constrain any content loaded into the WebView.

### 6. [MEDIUM] Release shrinking appears disabled

**Evidence**

```text
app/build.gradle.kts:9:            isMinifyEnabled = false
```

**Recommended fix:** Enable R8 for release builds, add focused keep rules, and exercise critical flows in a release-like test build.

### 7. [LOW] Development logging remains in source

**Evidence**

```text
app/src/main/java/dev/ship/risky/MainActivity.kt:11:        Log.d("RiskySample", "configured")
```

**Recommended fix:** Route logs through a release-aware logger and ensure sensitive values are never emitted.

### 8. [LOW] Release follow-up markers remain

**Evidence**

```text
app/src/main/java/dev/ship/risky/MainActivity.kt:8:        // TODO: remove test bridge before release.
```

**Recommended fix:** Review each marker before release and convert accepted debt into tracked work items.

## Coverage

This Lite scan checks common release configuration, secret exposure, network security, WebView, logging, and code-marker patterns. It does not inspect runtime behavior, dependency CVEs, merged manifests, Play policy declarations, or R8 reachability.
